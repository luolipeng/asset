using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class test3 : MonoBehaviour
{
   public Transform ls;
    // Start is called before the first frame update
    void Start()
    {
        //this.transform.RotateAround(this.transform.position, new Vector3(0,1,0), -90);
      //  this.transform.RotateAround(this.transform.position, ls.forward, -90);
    }

    // Update is called once per frame
   public void Update2()
    {
        
    }
    public void Update5()
    {

    }
}
